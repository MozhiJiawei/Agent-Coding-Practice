# 测试用例文件
# 完整断言引擎在 Story 6.1 中实现，当前仅用于链路验证

test_cases:
  - id: chat_hello
    type: Chat
    messages:
      - "你好"
    expect:
      has_response: true
      response_not_empty: true
    tags:
      - smoke
